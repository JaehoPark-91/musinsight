# Grafana - 메트릭(Mimir) + 로그(Loki) 통합 뷰어
adminUser: admin
adminPassword: "qwe1212!Q"
persistence:
  enabled: false
service:
  type: ClusterIP
datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Mimir
        type: prometheus
        access: proxy
        url: http://mimir.observability.svc.cluster.local:8080/prometheus
        isDefault: true
      - name: Loki
        type: loki
        access: proxy
        url: http://loki.observability.svc.cluster.local:3100
